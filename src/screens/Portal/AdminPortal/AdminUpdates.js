import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const AdminUpdates = () => {
  return (
    <View style={styles.container}>
      <Text>Admin Updates</Text>
    </View>
  );
};

export default AdminUpdates;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
